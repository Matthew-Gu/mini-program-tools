<view hidden="{{ !enable }}" class="popup-container">
	<view
		class="popup-mask {{ maskClass }}"
		style="{{ maskStyle }}"
		bind:tap="close"></view>
	<view class="popup-wrapper {{ popupClass }}" style="{{ popupStyle }}">
		<slot />
	</view>
</view>
