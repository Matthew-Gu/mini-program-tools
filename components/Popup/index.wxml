<view class="popup-container">
    <view wx:if="{{maskShow}}" class="popup-mask {{maskClass}}" style="{{maskStyle}}" bind:tap="close"></view>
    <view wx:if="{{popupShow}}" class="popup-wrapper {{popupClass}}" style="{{popupStyle}}">
        <slot />
    </view>
</view>